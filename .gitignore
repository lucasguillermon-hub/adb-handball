
# Estado local de wrangler (base D1 de prueba, caché)
.wrangler/
node_modules/

# Material de diseño de los sponsors (PDF, PSD, placas). Los logos para la web
# ya están exportados en fotos/sponsors/. No va al repo ni se publica.
6. SPONSORS 2026/

# Listas del club y archivos sueltos que no son del sitio
*.xlsx
*.exe

# Placas del fixture (para recortar los escudos de los rivales): no van al repo
placas-fixture/

# Material del club que sirve de fuente (planillas de horarios, etc.): no se publica
material-club/

# Configuración local de Claude Code (dev server para la vista previa)
.claude/
